📁 aider-operator-pack-template (folder.txt format)

FILE: README.md
FILE: agent.md
FILE: spec.md
FILE: prompt_plan.md
FILE: docs/WORKFLOW.md
FILE: Makefile
FILE: .gitignore
FILE: scripts/run_tests.sh
FILE: scripts/run_step.sh
FILE: LICENSE
FILE: .github/workflows/ci.yml
FILE: example/test.js
FILE __init__.py
FILE test/example.py
FILE ci.yml
FILE: WORKFLOW.md

# agent.md — Aider guardrails

You are a cautious, test-first coding assistant working in this repository.

## Non-negotiable rules
- Make **minimal, targeted changes**.
- Do **not** refactor unrelated code.
- Prefer **small functions** and clear names.
- **Write or update tests** for any behaviour change.
- After edits, **run verification** (tests/lint/typecheck if available).
- If tests fail, **fix only the failing behaviour**; do not rewrite working code.

## Output discipline
- Prefer small diffs.
- Avoid adding new dependencies unless explicitly required.
- If you must change a public interface, update docs and tests.

## Safety
- Never include secrets in code or docs.
- If a task is ambiguous, propose one conservative option and proceed.

FILE run_test.sh

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.coverage

# Node
node_modules/
.npm/
.pnpm-store/

# Logs
*.log

# Secrets
.env

File: run_step.sh

# prompt_plan.md

## Rules
- Each step must be **small** and **testable**.
- Each step should touch **1–2 files**.
- Each step must state **success criteria** (tests/examples).
- No step should rely on future steps.

---

## Step 1 — Project skeleton
Goal:
- Create minimal runnable skeleton and basic test harness.

Files:
- (list files)

Task:
- (one precise instruction)

Success criteria:
- `make test` passes (or your test command)

---

## Step 2 — First behaviour
Goal:
- Implement first small user-visible behaviour.

Files:
- 

Task:
- 

Success criteria:
- Add tests for the behaviour and make them pass.

---

## Step 3 — Edge cases
Goal:
- Add validation and edge case coverage.

Files:
-

Task:
-

Success criteria:
- Tests include invalid/edge cases.

FILE: README.md

# spec.md

## One-sentence goal
(What are you building?)

## Users / audience
(Who uses it? In what environment?)

## Definition of done
- [ ] Core user journeys work
- [ ] Tests cover key behaviours
- [ ] Lint/type checks pass (if applicable)
- [ ] README has setup + run instructions

## Constraints
- Language/runtime:
- Dependencies:
- Performance:
- Security/privacy:

## Non-goals
(What are you explicitly not doing?)

## Verification commands
(These are the commands Aider should run)
- Tests:
- Lint:
- Typecheck:

File: download

# Makefile

.PHONY: help test lint typecheck

help:
	@echo "Targets: test lint typecheck"
	@echo "Edit this Makefile to match your project."

# Default test target: try common tools.
# Prefer to replace with your project's actual command(s).

test:
	@./scripts/run_tests.sh

lint:
	@echo "No lint configured. Add a lint command here."

typecheck:
	@echo "No typecheck configured. Add a typecheck command here."
	
	
Aid-4_Template.txt
Displaying Aid-4_Template.txt.
